❯ нарисуй 2 машины состояний: fsm для file_vault и fsm для se_vault

● Прочитаю main.cc se_vault — file_vault у меня уже есть.

  Read 1 file (ctrl+o to expand)

● Теперь у меня есть всё для обеих машин состояний.

  ---
  FSM: file_vault

                               ┌─────────┐
                      ┌───────►│  INVALID │  (начальный запуск)
                      │        └────┬─────┘
                      │             │ image_fs_query: файл найден?
                      │      ┌──────┴──────┐
                      │   да │             │ нет
                      │      ▼             ▼
                      │  ┌────────┐   ┌─────────────┐
                      │  │ LOCKED │   │UNINITIALIZED│
                      │  └───┬────┘   └──────┬───────┘
                      │      │ passphrase     │ passphrase + размеры FS
                      │      │               ▼
                      │      │        ┌──────────────────┐
                      │      │        │SETUP_CREATE_IMAGE│  truncate_file
                      │      │        └──────┬───────────┘
                      │      │               │ ok
                      │      │        ┌──────▼──────────────────┐
                      │      │        │SETUP_INIT_TRUST_ANCHOR  │  tresor_init_trust_anchor
                      │      │        └──────┬──────────────────┘
                      │      │               │ ok
                      │      │        ┌──────▼───────────┐
                      │      │        │SETUP_TRESOR_INIT  │  tresor_init
                      │      │        └──────┬────────────┘
                      │      │               │ ok
                      │      │        ┌──────▼────────────┐
                      │      │        │SETUP_START_TRESOR │  tresor_vfs + sync_to_tresor_vfs_init
                      │      │        └──────┬────────────┘
                      │      │               │ ok
                      │      │        ┌──────▼──────┐
                      │      │        │SETUP_MKE2FS │  mke2fs
                      │      │        └──────┬───────┘
                      │      │               │ ok
                      │      │        ┌──────▼──────────────┐
                      │      │        │SETUP_READ_FS_SIZE   │  client_fs_query
                      │      │        └──────┬──────────────┘
                      │      │               │ размер получен
                      │      │         ┌─────┘
                      │      ▼         ▼
                      │  ┌─────────────────────────┐
                      │  │UNLOCK_INIT_TRUST_ANCHOR │  tresor_init_trust_anchor
                      │  └────────┬───────┬─────────┘
                      │      fail │       │ ok
                      │  (3с delay│       ▼
                      └──────────┘  ┌─────────────────────┐
                                    │UNLOCK_START_TRESOR  │  tresor_vfs + sync_to_tresor_vfs_init
                                    └──────────┬──────────┘
                                               │ ok
                                    ┌──────────▼──────────┐
                                    │UNLOCK_READ_FS_SIZE  │  client_fs_query
                                    └──────────┬──────────┘
                                               │ размер получен
                                               ▼
                                ┌──────────────────────────┐
                      ┌────────►│         UNLOCKED          │◄──────────────┐
                      │         └──────────────┬────────────┘               │
                      │                        │ пользователь lock          │
                      │                        │ (passphrase очищен)        │
                      │                        ▼                            │
                      │         ┌──────────────────────────┐                │
                      │         │       LOCK_PENDING        │  ждёт extend/ │
                      │         └──────────────┬────────────┘  rekey        │
                      │                        │ extend и rekey завершены   │
                      │                        ▼                            │
                      │         ┌──────────────────────────┐                │
                      │         │      START_LOCKING        │  lock_fs_tool  │
                      │         └──────────────┬────────────┘               │
                      │                        │ ok                         │
                      │                        ▼                            │
                      │         ┌──────────────────────────┐                │
                      │         │         LOCKING           │  lock_fs_query │
                      │         └──────────────┬────────────┘               │
                      │                        │ "succeeded"                │
                      └────────────────────────┘ → LOCKED ──────────────────┘
                                                    ▲
                                (следующий unlock) ─┘

  Параллельные под-машины в UNLOCKED:
  Extend: INACTIVE → ADAPT_IMAGE_SIZE → WAIT_FOR_TRESOR → SEND_REQUEST
                   → REQUEST_IN_PROGRESS → READ_FS_SIZE → RESIZE2FS → INACTIVE

  Rekey:  INACTIVE → WAIT_FOR_TRESOR → SEND_REQUEST → REQUEST_IN_PROGRESS → INACTIVE

  ---
  FSM: se_vault

           ┌──────┐
           │ start│  (конструктор)
           └──┬───┘
              │
              ▼
    ┌──────────────────┐
    │  CHECK_INIT_FLAG │  check_init_flag читает /initialized
    └────┬───────┬─────┘
    exit=1│       │ exit=0
   (нет  │       │ (есть /initialized)
   флага)│       │
         │       ▼
         │  ┌───────────────────────────┐
         │  │  UNLOCK_INIT_TRUST_ANCHOR │  tresor_init_trust_anchor
         │  └─────────────┬─────────────┘
         │                │ ok
         │  ┌─────────────▼─────────────┐
         │  │   UNLOCK_START_TRESOR     │  tresor_vfs + sync_to_tresor_vfs_init
         │  └─────────────┬─────────────┘
         │                │ ok
         │        ┌───────┴──────────┐
         │  fsck_ │                  │ fsck_
         │  apply │                  │ apply=false
         │  =true │                  │
         │        ▼                  │
         │  ┌──────────┐             │
         │  │  E2FSCK  │  e2fsck     │
         │  └──┬──┬────┘             │
         │  ok │  │ code=1,2         │
         │     │  │ (ошибки          │
         │     │  │  исправлены)     │
         │     │  ▼                  │
         │     │ ┌──────────┐        │
         │     │ │ UNMOUNT1 │  3с таймер, system_vfs без записи
         │     │ └────┬─────┘
         │     │      │ (таймер сработал)
         │     │      ▼
         │     │ ┌──────────┐
         │     │ │ UNMOUNT2 │  system_vfs с деактивацией
         │     │ └────┬─────┘
         │     │      │ system_vfs завершился
         │     │      └────────────────────────────┐
         │     │                                   │
         │     │ (повторный fsck)                  │
         │     │◄──────────────────────────────────┘
         │     │
         ▼     ▼
         │  ┌────────────────────────────────────────────────────┐
         ├──►                    UNLOCKED                        │
         │  │  tresor_trust_anchor_vfs + tresor_vfs +            │
         │  │  vfs_block + system_vfs (предоставляет File_system)│
         │  └──────────────────────────────┬─────────────────────┘
         │                                 │
         │       ┌─────────────────────────┤
         │       │ клиенты отключились     │ system_vfs
         │       │ (_had_clients &&        │ сам завершился
         │       │  num_clients=0)         │
         │       ▼                         │
         │  ┌──────────────────┐           │
         │  │ STOP_SYSTEM_VFS  │           │
         │  └────────┬─────────┘           │
         │           │ system_vfs завершился│
         │           └──────────┬──────────┘
         │                      ▼
         │           ┌──────────────────┐
         │           │  START_LOCKING   │  lock_fs_tool
         │           └──────────┬───────┘
         │                      │ ok
         │                      ▼
         │           ┌──────────────────┐
         └──────────►│     LOCKED       │  env.parent().exit(0)
                     └──────────────────┘  (se_vault завершается)

    При любой ошибке:
         → INVALID → env.parent().exit(1)

  ---
  Ключевые различия между двумя FSM

  ┌────────────────┬─────────────────────────────────────────────────────────────────────┬───────────────────────────────────────────────────────────────────────┐
  │                │                             file_vault                              │                               se_vault                                │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ Первый запуск  │ INVALID проверяет наличие образа через image_fs_query               │ CHECK_INIT_FLAG проверяет файл /initialized через отдельный компонент │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ Инициализация  │ Пользователь вводит passphrase интерактивно                         │ Passphrase зашит в конфиг (P53ud0_Pa55w0rd)                           │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ После setup    │ Сразу переходит в UNLOCKED                                          │ Создаёт флаг-файл (SETUP_INIT_FLAG), потом UNLOCKED                   │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ Блокировка     │ LOCK_PENDING → START_LOCKING → LOCKING → LOCKED (ждёт extend/rekey) │ STOP_SYSTEM_VFS → START_LOCKING → LOCKED → exit(0)                    │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ После lock     │ LOCKED — ждёт следующего unlock                                     │ LOCKED — сразу exit(0), процесс завершается                           │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ Проверка FS    │ Нет                                                                 │ E2FSCK после каждого unlock (если fsck_apply=true)                    │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ FS клиент      │ rump_vfs (ext2 через rump kernel)                                   │ system_vfs (отдельный VFS-сервер для Phantom)                         │
  ├────────────────┼─────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────┤
  │ Extend / Rekey │ Поддерживается                                                      │ Закомментировано                                                      │
  └────────────────┴─────────────────────────────────────────────────────────────────────┴───────────────────────────────────────────────────────────────────────┘

✻ Sautéed for 1m 34s

───────────────





























а где в se_vault состояния SETUP_*

● Я их пропустил в схеме — ветка exit=1 из CHECK_INIT_FLAG никуда не ведёт на рисунке. Исправляю se_vault FSM целиком.

  ---
  FSM: se_vault (исправленный)

           ┌───────┐
           │ start │  (конструктор)
           └───┬───┘
               ▼
    ┌───────────────────┐
    │  CHECK_INIT_FLAG  │  проверяет наличие /initialized
    └──┬──────────┬─────┘
  exit=1│          │exit=0
  (/ini-│          │(/initialized есть)
  alized│          │
  нет)  │          ▼
        │   ┌───────────────────────────┐
        │   │  UNLOCK_INIT_TRUST_ANCHOR │  tresor_init_trust_anchor
        │   └─────────────┬─────────────┘
        │                 │ ok
        │   ┌─────────────▼─────────────┐
        │   │   UNLOCK_START_TRESOR     │  tresor_vfs + sync_to_tresor_vfs_init
        │   └─────────────┬─────────────┘
        │                 │ ok
        │         ┌───────┴──────────┐
        │ fsck_   │                  │ fsck_apply
        │ apply   ▼                  │ =false
        │ =true ┌──────────┐         │
        │       │  E2FSCK  │         │
        │       └──┬──┬────┘         │
        │    code=0│  │code=1,2      │
        │          │  │(ошибки исп.) │
        │          │  ▼              │
        │          │ ┌──────────┐    │
        │          │ │ UNMOUNT1 │  3с таймер
        │          │ └────┬─────┘
        │          │      │ (таймер)
        │          │      ▼
        │          │ ┌──────────┐
        │          │ │ UNMOUNT2 │  system_vfs с деактивацией
        │          │ └────┬─────┘
        │          │      │ system_vfs завершился
        │          │      └──► (снова E2FSCK)
        │          │
        │          └──────────────────────────────────────┐
        │                                                  │
        ▼                                                  │
  ┌────────────────────────┐                              │
  │  SETUP_INIT_TRUST_     │  tresor_init_trust_anchor    │
  │  ANCHOR                │  (первичная инициализация)   │
  └───────────┬────────────┘                              │
              │ ok                                        │
  ┌───────────▼────────────┐                              │
  │  SETUP_TRESOR_INIT     │  tresor_init                 │
  │  (суперблоки, деревья) │                              │
  └───────────┬────────────┘                              │
              │ ok                                        │
  ┌───────────▼────────────┐                              │
  │  SETUP_START_TRESOR    │  tresor_vfs +                │
  │                        │  sync_to_tresor_vfs_init     │
  └───────────┬────────────┘                              │
              │ ok                                        │
  ┌───────────▼────────────┐                              │
  │  SETUP_MKE2FS          │  mke2fs                      │
  │  (форматирование ext2) │                              │
  └───────────┬────────────┘                              │
              │ ok                                        │
  ┌───────────▼────────────┐                              │
  │  SETUP_INIT_FLAG       │  создаёт файл /initialized   │
  └───────────┬────────────┘                              │
              │ ok                                        │
              └──────────────────────────────────────────►│
                                                          │
                                ┌─────────────────────────▼──────────────────────┐
                                │                  UNLOCKED                       │
                                │  tresor_trust_anchor_vfs + tresor_vfs +         │
                                │  vfs_block + system_vfs (→ File_system клиенту) │
                                └──────────────┬────────────────────┬─────────────┘
                                               │                    │
                                клиенты ушли   │                    │ system_vfs
                                (_had_clients  │                    │ сам завершился
                                 && clients=0) │                    │
                                               ▼                    │
                                ┌──────────────────────┐            │
                                │   STOP_SYSTEM_VFS    │            │
                                └──────────┬───────────┘            │
                                           │ system_vfs завершился  │
                                           └───────────┬────────────┘
                                                       ▼
                                           ┌───────────────────────┐
                                           │    START_LOCKING      │  lock_fs_tool
                                           └───────────┬───────────┘
                                                       │ ok
                                                       ▼
                                           ┌───────────────────────┐
                                           │        LOCKED         │  exit(0)
                                           └───────────────────────┘

    При любой ошибке → INVALID → exit(1)

  Суть ошибки в моём предыдущем ответе: ветка exit=1 из CHECK_INIT_FLAG была нарисована, но не соединена ни с каким состоянием. На самом деле она ведёт в SETUP_INIT_TRUST_ANCHOR — начало
  цепочки первичной инициализации, которая идёт параллельно с unlock-путём.


